# 项目记忆与维护手册

最后更新：2026-09-13
工作目录：`/Users/szy/Downloads/AWSzyAI.github.io`
正式站点：<https://awszyai.github.io/>

这份文档用于在下次维护时快速恢复上下文。仓库是公开的，因此只记录架构、决策和恢复流程，不记录真实财务明细、访问令牌或私密密钥。

## 1. 当前产品方向

这个仓库不只是个人介绍页，还包含长期使用的个人工具。

当前设计原则：

1. 主页保持浅色、墨绿色点缀、简约清晰。
2. 避免深色霓虹、玻璃拟态、渐变光效和“很多圆角卡片”的 AI landing page 风格。
3. 项目按用途排列，用目录式分割线呈现，不重复堆叠 Projects 与 Tools。
4. Money/Tax/Device 的真实数据绝不能作为默认值写进公开源码。
5. 未登录用户只看到模板；登录用户只看到自己的云端数据。

## 2. 当前文件与职责

### 公共页面

- `index.html`：公开合作主页；共享样式与移动导航在 `assets/site.css`、`assets/site.js`，仅主页加载的视觉增强在 `assets/motion.css`、`assets/motion.js`。
- `resume.html`：当前中英文简历；`assets/current-cv.png` 为网页渲染快照。
- `device.html`：设备、订阅、租赁、打卡、里程和收支记录，并接入云同步。
- `PhD.html`：长期情景建模。
- `blog.html`、`blog/`：博客入口和旧博客系统。
- `obsidian/`、`MkDocs/site/`、`Archive/`：静态内容与历史归档。

### 财务页面

- `money.html`：资产、未来现金流、预算、情景和贷款。
- `tax.html`：年度个税汇算测算。
- `cloud-sync.js`：Money/Tax 共用的 GitHub 登录和 Supabase 同步层。
- `cloud-config.js`：公开的 Supabase URL、Publishable key、所有者 GitHub 登录名。
- `supabase-setup.sql`：表、权限和 RLS 策略。
- `CLOUD_SETUP.md`：控制台配置、备份、恢复和故障排查。

## 3. 主页设计记录

主页在提交 `423a534` 中完成重构。

视觉规范：

- 背景：暖白 `#f5f4ee`。
- 正文：近黑绿 `#18211c`。
- 点缀：墨绿色 `#184d36`。
- 分割线：`#d7d9d2`。
- 标题恢复使用 Georgia / Times New Roman / 霞鹜文楷，正文使用原有系统字体与霞鹜文楷回退。
- 内容最大宽度 1160px。
- 2026-09-13 按用户要求参考 Blux 官网增强动效：首屏的个人介绍压缩为顶部横向信息栏：中文名 30–34px 为主，英文名 14–15px 作同行注释；简介依据用户的研究目标重写，强调脑网络建模/假设检验、连续意识状态和行为—光学神经数据对齐。研究演示占下方完整主区域，桌面图形约占研究区域 2/3，说明与主题切换占 1/3；当前身份保持下方横栏。顶部留白桌面 20px、手机 16px，不设置最小视口高度。
- Work 使用横向目录条目，不使用卡片容器。
- 墨绿色用于研究愿景与页尾联系区块。

维护主页时应保留清晰的阅读层级，动效围绕神经科学与工程身份展开。合作方向采用目录式分隔线，工程实践采用两列项目，个人工具放在页尾次要位置。

### 2026-09-13 视觉增强

- 保留原有配色、字体与正文内容。参考 Blux 的视觉中心和空间层次，使用原创 Canvas 脑网络建模、连续意识状态、脑—行为对齐三种可切换研究示意。按用户反馈重写研究目的，取消“涌现心智”等口号和“生成式视觉 · 非实验数据”表述；界面不保留装饰性说明或免责声明标签。
- 工程实践增加内联 SVG 行为追踪与脑机接口概念图；章节采用滚动入场、悬停反馈、导航章节标识与阅读进度线。
- `assets/research-models.js` 管理连续状态与随机行为/神经活动模型，`assets/research-scenes.js` 绘制研究场景，`assets/motion.js` 管理播放、切换与页面动效。脑模型为侧视脑形与沟回/小脑/脑干，支持局部扰动；意识图在同一时间轴呈现连续信号，使用绿色实线表示觉醒、赭色虚线表示注意；曲线与状态带共用时间轴，取消情景分类，支持时间拖动；行为模型含随机停走、加速度、转头、步态和柔性尾巴，与钙成像、神经事件和速度/钙曲线共用仿真时钟。
- 文案重点：神经网络建模与编码心智、通过假设检验探索意识和记忆；刻画持续意识状态；行为与光学神经数据对齐。
- 模型是用于网站说明研究方向的示意，不是拟合真实数据的神经模型或临床量表；细节与依据见 `assets/RESEARCH_VISUALS.md`。纯模型验证：`node --test tests/research-models.test.cjs`。
- 不依赖第三方动画库或网络素材。无 JavaScript 时正文可读，首屏显示静态 SVG；Canvas 不可用时保留静态图。
- 提供暂停/播放按钮；遵循 prefers-reduced-motion。Canvas 在离屏、页面隐藏和用户暂停时停止绘制。手机降低节点数，粗指针设备禁用视差交互。
- 样式与脚本单独加载，只影响主页；简历、财务工具及云同步逻辑不受影响。
- 当前身份横栏中各展示一次西湖大学、深圳医学科学院和 Blux 官方 logo，保留原色与比例；素材在 `assets/logos/`，来源见该目录 README。联合培养两家机构并列，Blux 与工程身份对应。按用户反馈去掉 Currently / 持续探索 / 2026 及向下探索，身份栏采用两个明确分隔的区块，每组身份文字在左、官方标识在右并排一行，取消 logo 在文字上方的排列，避免四列布局造成空白。

## 4. Money 页面模型

`money.html` 的状态对象主要包含：

- `rate`：美元兑人民币汇率。
- `current[]`：当前资产。
- `future[]`：预计收入。
- `budget[]`：计划支出。
- `rows[]`：资产构成、预算、情景和贷款行。

贷款行使用：

- `totalAmount`：贷款总额。
- `totalPeriods`：总期数。
- `paidPeriods`：已还期数。

派生值：

```text
月供 = 总额 ÷ 总期数
剩余期数 = 总期数 − 已还期数
已还金额 = 月供 × 已还期数
剩余金额 = 月供 × 剩余期数
```

贷款主条展示现金流覆盖情况；下方细进度条展示已还期数与百分比。Current 与 Forecast 使用两条独立虚线，与其他预算/现金流基准对齐。

公开默认数据位于 `defaultState`，必须始终保持脱敏。Money 当前遗留 key 为 `szy_finance_demo_v2_sanitized`。

## 5. Device 页面模型

`device.html` 的云端 payload 包含：

- `items[]`：实体资产、订阅、租赁与打卡项目。
- `ledger`：按日期保存的手动收入与支出。
- `mileage`：旧导出文件中的 CSV 与结构化里程行；即使当前界面没有单独编辑全部字段，也要原样保留，避免导入后丢失。
- `exportedAt`：最近生成 payload 的时间。

公开页面默认只包含“示例”项目。真实数据来源文件不进入 Git：

```text
C:\Users\szy\iCloudDrive\PhD\all_assets_2026-07-02_13_38_22.json
```

Device 的全量导入会替换 `items`、`ledger` 和 `mileage`，登录状态下随后自动保存到 `document_type = device`。卡片列数、财务列数与标签拆分开关属于界面偏好，可继续保留在 localStorage；真实资产和账本不再写入 localStorage。

## 6. Tax 页面模型

`tax.html` 使用年度账本结构：

```text
book
├── activeYear
├── years
│   └── YYYY
│       ├── incomes[]
│       └── deductions
└── migrations
```

每笔收入包含：

- 状态：已发生 `confirmed` / 预计 `projected`。
- 期间、来源、所得类型、备注。
- 申报收入 `gross`。
- 预缴税额 `prepaid`。
- 实际到手 `received`：默认按 `gross - prepaid`，也可手动校准。

所得类型：工资薪金、劳务报酬、稿酬、特许权使用费。

年度链路：

```text
申报收入
→ 按所得类型折算综合所得收入额
→ 减基本/专项/专项附加/其他扣除和捐赠
→ 全年应纳税所得额
→ 年度最终税额
→ 减预缴税额
→ 预计退税或补税
```

页面还展示：

- 全年实际到手合计，不含退税。
- 实际到手加退税后的汇算后总额。
- 年度最终税额按每笔年度收入额占比分摊后的“分摊应缴”。
- `分摊差额 = 实际预缴 − 分摊应缴`：红色表示多缴，绿色表示少缴。该值只用于分析，不是法定月度税额。
- 可拖动并记忆表格列宽。
- 年度扣除项目默认折叠。

Tax 数据 key：`szy_tax_refund_planner_v2`。列宽仅保存在本机 `szy_tax_column_widths_v1`，不属于财务数据。

## 7. 云同步架构

### 登录与路由

1. 页面加载 Supabase JS、`cloud-config.js` 和 `cloud-sync.js`。
2. 未登录时应用脱敏模板。
3. 点击“GitHub 登录”后，经 GitHub OAuth 返回 Supabase callback。
4. Supabase 恢复会话后按当前用户 ID 查询数据。
5. Money、Tax、Device 分别使用 `document_type = money`、`tax`、`device`。

### 数据表

`public.user_documents` 的主键为 `(user_id, document_type)`：

```text
user_id       auth.users.id
document_type money | tax | device
payload       jsonb
updated_at    timestamptz
```

RLS 强制所有 select/insert/update/delete 都满足 `auth.uid() = user_id`。匿名角色没有表权限。

### 保存行为

- 用户修改数据后调用 `scheduleSave`，650ms 防抖后 upsert。
- 手动同步调用 `saveNow`。
- 未登录时不会保存财务数据，只显示模板。
- 每个 GitHub 用户拥有独立行；其他账号不会看到所有者数据。

### 加密边界

- 网络传输使用 HTTPS。
- Supabase 提供数据库静态加密与平台备份。
- 当前不是客户端端到端加密：Supabase 项目管理员仍具备数据库管理能力。
- 如果以后要求“平台管理员也看不到明文”，需要增加用户侧密钥和客户端加密，不能只依赖 RLS。

## 8. 数据事故与恢复状态

### 发生了什么

2026-08-04 为避免真实财务数据继续存在于公开 Git 历史，执行了安全历史重置：

- `4b85da0`：新的安全根提交，公开默认数据改为模板。
- `0569bea`：写入 Supabase 公开配置并启用线上云同步。

历史重置后，原来写在旧版 Tax 默认值中的个人账本不再出现在远端可达历史中，但本机 Git 对象仍可恢复。旧数据在本机旧提交 `cb50d4b` 的 `tax.html` 中被找回。

首次云端初始化出现了一个重要问题：如果云端已经存在模板行，`cloud-sync.js` 会优先读取远端 payload，不再尝试迁移旧 localStorage。因此模板可能覆盖用户对“数据已迁移”的预期。

Money 还有额外风险：当前 `legacyStorageKey` 是新的脱敏 key，而不是旧版真实数据使用的 key，因此不能假定旧 Money localStorage 会自动迁移。

### 当前恢复状态

- 旧 Tax 账本已从本机 Git 对象提取为一次性恢复 JSON。
- 恢复文件只在本机临时目录，未提交到公开仓库：
  `C:\Users\szy\AppData\Local\Temp\szy-tax-recovery-2026-2027.json`
- 截至本文件更新时间，该恢复文件尚未确认导入 Supabase 用户行。
- 恢复必须在所有者 GitHub 会话登录后，通过 Tax 页“导入”完成并确认右下角显示“已同步”。

不要把恢复 JSON 加入 Git。完成恢复后应先导出一份用户自行保管的备份，再删除临时文件。

## 9. 下次继续时的优先事项

1. 完成 Tax 恢复：用 `AWSzyAI` 登录 Tax 页面，导入临时恢复 JSON，检查 2026/2027 年份和汇总结果，等待云端同步。
2. 立刻从 Tax 页“导出 JSON”保存新的私有备份。
3. 检查 Money 是否仍能在原浏览器找到旧数据；不要在未备份前反复刷新或清理浏览器数据。
4. 改进首次迁移策略：当远端只有模板时，提供明确的“导入旧本机数据/保留云端模板”选择，禁止静默覆盖。
5. 为 Money 与 Tax 增加可见的“最近云端保存时间”和手动导出提醒。
6. 在 Supabase 执行更新后的 `supabase-setup.sql`，启用 `document_type = device`，再用所有者账号导入 Device 私有 JSON。

## 10. 安全操作规则

- 公开仓库中只能存在脱敏模板。
- 不提交 Money/Tax/Device JSON 备份、截图中的真实金额、access token、refresh token、GitHub Client Secret 或 Supabase `service_role` key。
- `cloud-config.js` 只允许 Project URL 和 Publishable key。
- 修改 `supabase-setup.sql` 后必须重新验证匿名读取被拒绝、不同账号互相不可见。
- 在删除 localStorage 前，必须确认云端写入成功并能在刷新后重新读取。
- 历史重写只用于清除已公开的敏感信息；执行前要保留本地恢复点。

## 11. 开发与验证流程

### 本地检查

```powershell
git diff --check
node --check cloud-config.js
node --check cloud-sync.js
python -m http.server 4173
```

浏览器检查：

- 主页桌面与移动布局无横向溢出。
- 所有导航和项目链接有效。
- Money/Tax/Device 未登录时只显示模板。
- 登录后状态显示 GitHub 用户名与“已同步”。
- 修改数据、等待自动保存、刷新后数据仍存在。
- 用第二个账号登录时只能看到该账号自己的模板或数据。

### 部署检查

```powershell
git push origin main
gh run list --limit 5 --json status,conclusion,headSha,url
```

GitHub Pages 成功后，用带查询参数的地址绕过浏览器缓存：

```text
https://awszyai.github.io/?deploy=<commit>
```

## 12. 关键提交

| 提交 | 含义 |
| --- | --- |
| `423a534` | 主页改为浅色、墨绿色、目录式简约设计 |
| `0569bea` | 配置 Supabase 云同步 |
| `4b85da0` | 安全重置公开历史，移除真实财务默认数据 |
| `cb50d4b` | 本机仍可访问的旧 Tax 实现与恢复来源，不应重新推到公开远端 |

## 13. 下次会话建议开场

可以直接告诉维护者：

> 请先阅读仓库根目录的 `PROJECT_MEMORY.md` 和 `CLOUD_SETUP.md`。先检查 Tax 恢复是否完成，再做其他财务页面改动。不要把本机恢复 JSON 或真实金额提交到 Git。

## 14. 2026-09-11 公开身份与合作主页更新

- 当前公开身份：时子延 / Ziyan Shi；Blux 全栈工程师；西湖大学与深圳医学科学院（SMART）联合培养博士生，神经科学方向。SMART 是深圳医学科学院。
- 内容顺序：合作方向 → 工程实践 → 研究与论文 → 关于我 → 工具与历史笔记 → 联系方式。
- 工程经历为小鼠视觉流程、EEG / SSVEP 机械臂原型；神经动力学、学习记忆与意识体验属于当前训练或研究兴趣，不能写成已经取得的成果。不要把已停止的旧项目列为当前主线。
- 主联系方式统一为 `shiziyan@westlake.edu.cn`，包括历史 HTML、RSS、旧博客演示身份与简历图片。只更新本网站，不修改公司主页或私人 Obsidian 源文件。
- `resume.html` 为简历的当前入口。旧的中英文 Obsidian 简历保留历史正文，增加当前版本提示、canonical 与 noindex。五个旧简历 PNG 路径使用新版网页快照，避免继续展示过期邮箱。
- 搜索入口新增中文姓名与英文姓名的标题、描述、Open Graph、Person/ProfilePage 结构化信息，以及根目录 `robots.txt` / `sitemap.xml`。
- 公开核对来源：Blux 官网、SMART 联培博士官方说明、SciOpen 论文页（DOI 10.1016/j.bidere.2025.100044）、Wiley 论文页（DOI 10.1002/cpe.8128）。第一篇为 2025 在线发表、2026 卷期；共同第一作者信息已核对。
- 内容综合了截至 2026-09-09 的 Obsidian Profile / Statement、轮转与研究规划，并回查原始项目记录。私人规划原文、未公开课题细节、身份号码、财务和内部聊天没有写入公开内容。
- 新版简历 PNG 通过浏览器渲染 `resume.html` 生成，导出时需检查整页包含页尾新邮箱，不能截断。

### 同日内容反馈

- 用户要求专业、直接的公开表达，避免“正在做的事情，不需要包装成口号”等带有提示词痕迹的文案。
- Now 提升为首页主要区块：Blux startup；SMART & Westlake PhD Rotation（全英文课程、全脑神经动力学、睡眠与意识）；睡眠—意识状态动态端到端模型；神经科学学习与综述；连接组与转录组约束的全脑模拟假设检验模型；耦合振荡子与旋转波探索。模型方向按照探索/构想呈现，不写成已完成成果。
- 论文统一标题为“已发表论文”，每篇仅一段引用，自己的名字加粗；补齐 2023 年 BESC 论文。不使用逐篇介绍、贡献标签或年份侧栏。

- 用户要求隐藏具体轮转课题组名称：公开首页与简历只写博士课程和科研轮转，图片快照同步更新。

- 2026-09-11 用户要求恢复品牌资源与原字体：导航 logo 和 favicon 均引用根目录 `szy.ico`；字体恢复原主页的 Georgia / Times New Roman / LXGWWenKai 标题与原正文字体栈。保留新版内容、布局和紧凑顶部间距。

### 2026-09-13 产品官网方向

用户将「时子延」作为个人品牌运营，要求产品官网而非博客格子布局。首页以整屏脑网络动画为主视觉，中文品牌突出、英文为小注释；后续顺序为使命、产品、战略方向、技术架构、公开研究和经历。产品逐个大幅展开，保留 Blux 参与关系与原型状态；使命是建模、编码与检验心智，探索意识与记忆。技术架构用采集 → 建模 → 执行的连续工作流表达。官方 logo 仍各一次，在身份文字右侧横排。纸色、墨绿色与原字体延续。意识图用双色双曲线与状态区间同步，注意游移较明显起伏、专注收敛、脑雾的觉醒与注意分离；不再区分日常与意识障碍视图。前端不出现装饰性的 AI 项目提示。

- 后续反馈确认：留白源于固定 max-width 和画布缩放，不是字体。首屏最终为贯穿浏览器宽高的墨绿色动态场景，左侧使命、右侧放大的脑网络与全屏流动纤维；正文取消 1200px 上限，采用随视口变化的边距。首屏顶部导航同色，滚动后恢复纸色。保留原字体；手机使用上下构图。

- 小鼠视觉升级为 2.5D：斜俯视场地、具有高度的身体/头部/耳朵网格、固定光照、前后遮挡、贴地阴影及脚爪抬落。头部和柔性尾巴仍跟随原行为模型，神经事件与钙成像继续使用同一时钟；手机降低网格精度。

- 用户否定手绘解剖图，要求科技感与可信人脑外形。现改用 Brainder / Anderson Winkler 的 MRI 重建表面，原生 WebGL 绘制皮质沟回、立体线网、动态传播和轻微视角变化；默认「神经网络」，「脑区结构」显示透明皮质与海马、杏仁核等深部结构。下丘脑仅标注区域，不冒称精确分割。几何遵循 CC BY-SA 3.0，页尾链接独立来源页；动画非测量连接组。静态降级图也由同一网格投影生成。
- 光学成像根据用户提供的参考图改为「双侧小鼠脑轮廓＋圆形颅窗」，荧光、组织细纹及血管限定在脑形内，保留中线、颅窗边圈与行为同步时间轴。手机与桌面均已核对。

- 用户要求行为场景成为主体：扩大场地至主要画幅，光学颅窗、神经事件及同步曲线缩为周围悬浮仪器；手机也保留整宽场地。加入共享坐标的立柱、方块和矮平台，小鼠按物体目标探索、避障并停下嗅探。8 项模型检查通过，包括多随机种子下的障碍物间距、边界、无跳位、主动嗅探及神经同步。

- 个人介绍删去「曾参与藻类目标检测」，保留已发表论文记录。加入「一生一芯」项目学习、jyy 操作系统课程、大模型、知识图谱和基于 Obsidian 的知识管理学习，不表述为已完成的项目或成果。

- 最新方向：三种视觉统一墨绿科技风。脑网络不再用视图按钮，采用 28 秒自动淡入淡出循环展示网络与脑区结构，支持暂停。意识状态加入 Apple Watch 与 EEG 采集动画、觉醒/注意双色历史与未来曲线、预测范围及较晚日程对照；咖啡、晚餐、入睡时间可调整，明确为样例日程/情景预测。桌面行为场地采用更开阔的视角填充宽屏，深色场地与发光轨迹连接悬浮光学窗及信号窗；手机保留原构图。

- 用户要求进一步提高行为场景质感：新增 `mouse-renderer.js` 原生 WebGL 渲染，改为平滑低伏的身体、尖吻、薄耳、眼睛高光、细脚爪与柔性尾巴；障碍物采用圆滑边缘、统一材质光照及深度遮挡，原 Canvas 网格仅作降级。光学颅窗、神经事件、同步时间轴显著放大并叠入场地，文字和曲线同步增大；行为模式隐藏导航下重复的姓名，仅保留「行为追踪」标题并上移画幅。行为模型与神经时钟不变，10 项模型检查通过。

- Apple Watch 外观改用苹果开发者站公开的 Series 11 产品边框图（来源与处理见 `assets/devices/README.md`），保留真实表壳、表冠和织物表带，设备正面静置。`watch-display.js` 在正式边框内绘制系统无衬线屏幕：当前状态、双色觉醒/注意趋势、未来两小时状态，跟随现有时间与日程模型更新；删除旧手绘手表、圆环和装饰波形。页尾新增设备素材来源。

### 2026-09-13 SEO 与正式发布

- 首页和简历保留可直接抓取的静态正文，补齐 WebSite / ProfilePage / Person 身份关联、robots meta、独立 canonical、分享元数据及 1200 × 630 品牌分享图。根 robots.txt 开放抓取，sitemap.xml 聚焦首页与当前简历；.nojekyll 明确静态发布。
- 新增 `scripts/check-seo.py` 做抓取与元数据检查，`scripts/submit-indexnow.py` 在确认 Pages 线上文件与本地一致后向 IndexNow 通知两页更新。公开 key 位于根目录 `indexnow-key.txt`，不涉及用户登录凭据。
- 用户已授权 git push 部署，并明确 Google / 百度站长账号验证稍后处理。不得将 IndexNow 接收回执表述为所有搜索引擎已经收录。验证与维护步骤见 `SEO.md`。
